# CSV 는 줄끝을 LF 로 못 박는다. 예전엔 작업트리가 CRLF 라 파일을 다시 쓸 때마다
# **전 줄이 바뀐 것으로** 잡혔고(3만 줄 diff), 그 잡음 때문에 커밋에서 빼게 됐다.
# 그러면 Actions 가 restore_db() 로 복원할 때 그날 데이터가 없어 사이트가 되돌아간다
# (2026-09-18 실제 사고). 여기서 끝낸다.
data/*.csv text eol=lf
data/**/*.csv text eol=lf
